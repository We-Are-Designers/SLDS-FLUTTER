{
  "installCommand": "git clone https://github.com/flutter/flutter.git -b stable --depth 1 _flutter && export PATH=\"$PATH:$PWD/_flutter/bin\" && flutter doctor && flutter pub get && dart run build_runner build -d",
  "buildCommand": "export PATH=\"$PATH:$PWD/_flutter/bin\" && flutter build web --release",
  "outputDirectory": "build/web"
}
